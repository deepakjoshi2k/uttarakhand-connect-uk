import { createFileRoute } from '@tanstack/react-router'; import { PageHero,ProgramCard,SectionHeading,meta } from '@/components/site/primitives'; import { programs } from '@/data/programs'; export const Route=createFileRoute('/programs')({head:()=>meta('Our programmes','Education, wellbeing, cultural and relief programmes serving Uttarakhand communities.'),component:Programs});function Programs(){return <main><PageHero title="Our programmes" copy="Practical care shaped by community knowledge."/><section className="section-pad"><div className="container-site"><SectionHeading eyebrow="Four commitments" title="Where your support goes"/><div className="grid gap-5 md:grid-cols-2">{programs.map(p=><ProgramCard key={p.slug} item={p}/>)}</div></div></section></main>}
