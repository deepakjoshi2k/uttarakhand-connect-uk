import { createFileRoute,notFound } from '@tanstack/react-router'; import { Button } from '@/components/ui/button'; import { PageHero,SectionHeading,meta } from '@/components/site/primitives'; import { programs } from '@/data/programs'; import { gallery } from '@/data/gallery'; export const Route=createFileRoute('/programs/$slug')({loader:({params})=>{const item=programs.find(p=>p.slug===params.slug);if(!item)throw notFound();return item},head:({loaderData})=>meta(loaderData?.title||'Programme','Learn about this UKDB UK community programme.'),component:Detail});function Detail(){const p=Route.useLoaderData();return <main><PageHero title={p.title} crumb={`Programmes / ${p.title}`} copy={p.impact}/><section className="section-pad"><div className="container-site"><SectionHeading eyebrow={p.eyebrow} title="What this programme makes possible"/><p className="max-w-3xl text-lg text-muted-foreground">{p.description} We work through trusted local partners, agreed outcomes and transparent reporting to make every contribution count.</p><div className="my-14 grid gap-5 sm:grid-cols-3">{p.stats.map(s=><div key={s.label} className="rounded-2xl bg-secondary p-7 text-center"><strong className="font-display text-4xl text-primary">{s.value}</strong><p className="text-sm text-muted-foreground">{s.label}</p></div>)}</div><div className="grid gap-4 sm:grid-cols-3">{gallery.slice(0,3).map(g=><img key={g.caption} src={g.src} alt={g.alt} loading="lazy" className="aspect-square w-full rounded-2xl object-cover"/>)}</div></div></section><section className="bg-accent py-16 text-accent-foreground"><div className="container-site flex flex-col gap-6 md:flex-row md:items-center md:justify-between"><h2>Stand behind {p.title}.</h2><Button asChild variant="secondary"><a href="https://example.org/donate">Support this programme</a></Button></div></section></main>}
