import { createFileRoute } from '@tanstack/react-router'; import { useState } from 'react'; import { Link } from '@tanstack/react-router'; import { Button } from '@/components/ui/button'; import { EventCard,PageHero,SectionHeading,meta } from '@/components/site/primitives'; import { events } from '@/data/events'; export const Route=createFileRoute('/events')({head:()=>meta('Events','Upcoming and past Uttarakhand community gatherings across the United Kingdom.'),component:Events});function Events(){const[tab,setTab]=useState<'upcoming'|'past'>('upcoming');const shown=events.filter(e=>tab==='past'?e.past:!e.past);return <main><PageHero title="Events" copy="Meet, celebrate, learn and lend a hand."/><section className="section-pad"><div className="container-site"><div className="mb-10 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><SectionHeading eyebrow="Gather together" title="What’s happening"/><div className="flex rounded-lg border p-1" role="tablist"><Button variant={tab==='upcoming'?'default':'ghost'} onClick={()=>setTab('upcoming')} role="tab">Upcoming</Button><Button variant={tab==='past'?'default':'ghost'} onClick={()=>setTab('past')} role="tab">Past</Button></div></div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{shown.map(e=><EventCard key={e.slug} item={e}/>)}</div><Button asChild variant="outline" className="mt-10"><Link to="/gallery">View event gallery</Link></Button></div></section></main>}
