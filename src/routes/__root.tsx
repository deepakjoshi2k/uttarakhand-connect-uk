import { QueryClient,QueryClientProvider } from '@tanstack/react-query'; import { Outlet,Link,createRootRouteWithContext,useRouter,HeadContent,Scripts } from '@tanstack/react-router'; import { useEffect,type ReactNode } from 'react'; import appCss from '../styles.css?url'; import { reportLovableError } from '../lib/lovable-error-reporting'; import { Header,Footer } from '@/components/site/layout'; import { Toaster } from '@/components/ui/sonner';
function NotFoundComponent(){return <main className="grid min-h-screen place-items-center bg-background p-6 text-center"><div><h1 className="text-primary">404</h1><p className="mt-3 text-muted-foreground">This path has wandered beyond the ridge.</p><Link to="/" className="mt-6 inline-block font-semibold text-accent">Return home</Link></div></main>}
function ErrorComponent({error,reset}:{error:Error;reset:()=>void}){const router=useRouter();useEffect(()=>reportLovableError(error,{boundary:'root'}),[error]);return <main className="grid min-h-screen place-items-center p-6 text-center"><div><h1>This page didn’t load</h1><button onClick={()=>{router.invalidate();reset()}} className="mt-6 font-semibold text-accent">Try again</button></div></main>}
export const Route=createRootRouteWithContext<{queryClient:QueryClient}>()({head:()=>({meta:[{charSet:'utf-8'},{name:'viewport',content:'width=device-width, initial-scale=1'},{property:'og:type',content:'website'},{property:'og:site_name',content:'UKDB UK'},{name:'twitter:card',content:'summary_large_image'}],links:[{rel:'stylesheet',href:appCss},{rel:'preconnect',href:'https://fonts.googleapis.com'},{rel:'preconnect',href:'https://fonts.gstatic.com',crossOrigin:'anonymous'},{rel:'stylesheet',href:'https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,600;9..144,700&family=Inter:wght@400;500;600&display=swap'},{rel:'icon',href:'/favicon.png',type:'image/png'}]}),shellComponent:RootShell,component:RootComponent,notFoundComponent:NotFoundComponent,errorComponent:ErrorComponent});
function RootShell({children}:{children:ReactNode}){return <html lang="en"><head><HeadContent/></head><body>{children}<Scripts/></body></html>}
function RootComponent(){const{queryClient}=Route.useRouteContext();return <QueryClientProvider client={queryClient}><Header/><Outlet/><Footer/><Toaster position="top-center" richColors/></QueryClientProvider>}
